---
phase: 03-brand-site-build
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - site/src/content/docs/getting-started.mdx
  - site/src/content/docs/api-reference.mdx
  - site/src/content/docs/wallet-setup.mdx
  - site/src/assets/wallet/placeholder-metamask-install.png
  - site/src/assets/wallet/placeholder-add-base.png
  - site/src/assets/wallet/placeholder-get-usdc.png
  - site/src/assets/wallet/placeholder-export-key.png
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "Getting Started guide walks through free mode (screenshot of example.com) then paid mode (crypto sentiment for BTC)"
    - "All 4 MCP client configs (Claude Desktop, Claude Code, Cursor, Windsurf) are copy-pasteable"
    - "Every npx invocation uses the -y flag"
    - "API reference documents all 6 MCP tools with parameter tables and examples"
    - "Wallet setup guide covers MetaMask install through private key export"
    - "Common Issues section covers npx -y, @x402/fetch vs x402-fetch, insufficient USDC, Node version"
  artifacts:
    - path: "site/src/content/docs/getting-started.mdx"
      provides: "Free mode + paid mode quickstart with 4 MCP client configs"
      contains: "npx -y"
    - path: "site/src/content/docs/api-reference.mdx"
      provides: "All 6 MCP tools with params, returns, examples"
      contains: "x402_screenshot"
    - path: "site/src/content/docs/wallet-setup.mdx"
      provides: "MetaMask → Base → USDC → private key export guide"
      contains: "MetaMask"
  key_links:
    - from: "site/src/content/docs/getting-started.mdx"
      to: "site/src/content/docs/wallet-setup.mdx"
      via: "MDX link in paid mode section"
      pattern: "wallet-setup"
    - from: "site/src/content/docs/getting-started.mdx"
      to: "site/src/content/docs/api-reference.mdx"
      via: "MDX link to full API reference"
      pattern: "api-reference"
---

<objective>
Write all three documentation pages — Getting Started, API Reference, and Wallet Setup. These are Starlight MDX pages that provide the complete developer experience from first install to making paid API calls.

Purpose: Documentation is the primary conversion path. A developer who reads Getting Started should be able to go from zero to a working x402 MCP server in under 5 minutes.
Output: Three complete MDX documentation pages with all technical content, code examples, and cross-references.
</objective>

<execution_context>
@/Users/jameswisdom/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jameswisdom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-brand-site-build/03-RESEARCH.md
@.planning/phases/03-brand-site-build/03-01-SUMMARY.md
@assets/brand-guidelines.md
@src/index.ts
</context>

<interfaces>
<!-- All 6 MCP tools from src/index.ts — executor must document each one -->

1. x402_network_info — FREE
   Params: none
   Returns: network info, wallet status, API health, pricing

2. x402_screenshot — $0.01
   Params: url (string, required), width (int, 320-3840, default 1280), height (int, 240-2160, default 720), full_page (bool, default false), format (png|jpeg|webp, default png)
   Returns: base64 image data

3. x402_pdf_extract — $0.01
   Params: pdf_url (string, required)
   Returns: extracted text, page count, metadata

4. x402_sentiment — $0.01
   Params: coin (string, regex /^[A-Z0-9]{1,10}$/i, required)
   Returns: sentiment score (-1 to 1), confidence, sources, analysis

5. x402_market_overview — $0.05
   Params: none
   Returns: market-wide sentiment, top movers, trend analysis

6. x402_intelligence — $0.10
   Params: coin (string, regex /^[A-Z0-9]{1,10}$/i, required)
   Returns: comprehensive analysis (CoinGecko, DeFiLlama, CryptoPanic, Fear & Greed, GitHub)

MCP Client Config Pattern (all 4 clients):
- Claude Desktop: JSON with mcpServers.x402 = { command: "npx", args: ["-y", "x402-mcp-server"] }
- Claude Code: claude mcp add x402 npx -y x402-mcp-server
- Cursor: JSON with mcpServers.x402 = { command: "npx", args: ["-y", "x402-mcp-server"] }
- Windsurf: JSON with mcpServers.x402 = { command: "npx", args: ["-y", "x402-mcp-server"] }

Paid mode env var addition to Claude Desktop config:
{ "mcpServers": { "x402": { "command": "npx", "args": ["-y", "x402-mcp-server"], "env": { "X402_PRIVATE_KEY": "0xYOUR_PRIVATE_KEY" } } } }

Install command: npx -y github:jameswilliamwisdom/x402-mcp-server
(GitHub direct install — npm registry publish deferred per Phase 2 decision)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write Getting Started guide with free and paid mode paths</name>
  <files>site/src/content/docs/getting-started.mdx</files>
  <action>
Replace the placeholder getting-started.mdx with the complete guide. Use Starlight MDX components: Steps, Tabs, TabItem, Aside.

**Frontmatter:**
```yaml
---
title: Getting Started
description: Install x402 MCP server and start using pay-per-use APIs in under 5 minutes.
---
```

**Structure:**

1. **Introduction** (2-3 sentences):
   - "x402 gives your AI agent access to pay-per-use APIs — screenshots, PDF extraction, and crypto intelligence. One install, no API keys."
   - Mention free test mode available immediately, paid mode with a USDC wallet.

2. **Quick Start — Free Mode** (using Steps component):
   Step 1: "Add the server to your MCP client" — use Tabs with `syncKey="mcp-client"` showing all 4 client configs:
   - Claude Desktop tab: JSON config block
     ```json
     {
       "mcpServers": {
         "x402": {
           "command": "npx",
           "args": ["-y", "x402-mcp-server"]
         }
       }
     }
     ```
     Note: "Add to `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS) or `%APPDATA%\Claude\claude_desktop_config.json` (Windows)"
   - Claude Code tab: `claude mcp add x402 npx -y x402-mcp-server`
   - Cursor tab: JSON config (same structure as Claude Desktop) — add to `.cursor/mcp.json`
   - Windsurf tab: JSON config (same structure) — add to Windsurf MCP settings

   Step 2: "Restart your MCP client" — brief note that the client needs restart to pick up new server config.

   Step 3: "Try your first API call" — example: `"Take a screenshot of example.com"` — explain that in free mode, screenshots are limited to example.com, example.org, and httpbin.org.

   CRITICAL: Add Aside type="caution" title="Always use -y": "Run `npx -y x402-mcp-server`, not `npx x402-mcp-server`. Without `-y`, npx prompts for confirmation — which breaks MCP's stdio transport."

3. **Paid Mode** (using Steps component):
   Step 1: "Set up a wallet" — link to [Wallet Setup Guide](/wallet-setup/) for full instructions. Brief summary: need USDC on Base network.

   Step 2: "Add your private key" — show updated config with `env` block:
   - Tabs with syncKey="mcp-client" showing all 4 clients with X402_PRIVATE_KEY env var added
   - Claude Desktop: JSON with `"env": { "X402_PRIVATE_KEY": "0xYOUR_PRIVATE_KEY" }`
   - Claude Code: `claude mcp add x402 --env X402_PRIVATE_KEY=0xYOUR_PRIVATE_KEY npx -y x402-mcp-server`
   - Cursor: JSON with env block
   - Windsurf: JSON with env block

   Step 3: "Make a paid API call" — example: `"What's the crypto sentiment for BTC?"` — explain this uses x402_sentiment at $0.01 USDC.

   Aside type="tip": "Your first call? Start with x402_network_info — it's free and shows all available APIs with their status."

4. **Common Issues** (inline section at bottom, NOT separate page):
   - **npx without -y**: "If your MCP client shows 'unexpected token' errors, check that your config uses `npx -y`, not just `npx`."
   - **@x402/fetch vs x402-fetch**: "The package name is `x402-fetch` (no scope). `@x402/fetch` is a placeholder stub that won't work."
   - **Insufficient USDC**: "Paid tools require USDC on Base. Check your balance with a Base block explorer. Even $1 covers 100 screenshot calls."
   - **Node version**: "x402-mcp-server requires Node 18+. Run `node --version` to check."
   - **Server not appearing**: "After adding the config, restart your MCP client completely. Claude Desktop requires a full quit and reopen."

5. **Next Steps**:
   - [API Reference](/api-reference/) — full documentation for all 6 tools
   - [Wallet Setup](/wallet-setup/) — detailed wallet configuration guide

CRITICAL: Every single `npx` reference MUST include `-y`. Verify by searching the file before considering this task done.

Voice: Approachable for tutorials per brand guidelines. "Getting started should feel like a friend walking you through it."
  </action>
  <verify>
    <automated>cd /Users/jameswisdom/projects/x402-mcp-server/site && npm run build 2>&1 | tail -3 && test -f dist/getting-started/index.html && ! grep -q '"npx", "x402-mcp-server"' src/content/docs/getting-started.mdx && grep -c 'npx -y' src/content/docs/getting-started.mdx | xargs test 1 -le && echo "PASS: getting-started built, all npx have -y"</automated>
  </verify>
  <done>
    - Getting Started guide has free mode path (screenshot of example.com) and paid mode path (crypto sentiment for BTC)
    - All 4 MCP client configs present in synced Tabs (Claude Desktop, Claude Code, Cursor, Windsurf)
    - Every npx invocation includes -y flag
    - Caution aside warns about npx -y requirement
    - Common Issues section covers all 4 items: npx -y, @x402/fetch, insufficient USDC, Node version
    - Links to Wallet Setup and API Reference
    - Builds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Write API reference for all 6 MCP tools</name>
  <files>site/src/content/docs/api-reference.mdx</files>
  <action>
Replace the placeholder api-reference.mdx with the complete API reference. Single page with one section per tool (per research recommendation — 6 tools is small enough for one page).

**Frontmatter:**
```yaml
---
title: API Reference
description: Complete reference for all 6 x402 MCP tools — parameters, returns, pricing, and examples.
---
```

**Structure:**

1. **Introduction**: Brief overview — "x402 exposes 6 MCP tools. All tools work in free test mode without a wallet. Paid mode unlocks full capabilities."

2. **One section per tool** (in order: network_info, screenshot, pdf_extract, sentiment, market_overview, intelligence):

Each tool section follows this pattern:
```
## tool_name

[One sentence description]

**Cost:** $X.XX USDC on Base | Free in test mode

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| ... | ... | ... | ... | ... |

**Returns:** [Brief description of return data]

**Example:**

> "Ask your AI agent: '[conversational prompt]'"

[JSON example showing tool call with arguments]
```

**Specific tool documentation:**

**x402_network_info** (FREE):
- No parameters
- Returns: network info, wallet status, API list with health
- Example: "What APIs are available on the x402 network?"

**x402_screenshot** ($0.01):
- Params: url (string, required), width (int, 320-3840, default 1280), height (int, 240-2160, default 720), full_page (boolean, default false), format (png|jpeg|webp, default png)
- Returns: base64 image data
- Example: "Take a screenshot of stripe.com's pricing page"
- Note: Free mode limited to example.com, example.org, httpbin.org

**x402_pdf_extract** ($0.01):
- Params: pdf_url (string, required)
- Returns: extracted text, page count, metadata
- Example: "Extract the text from this SEC filing PDF"

**x402_sentiment** ($0.01):
- Params: coin (string, required — ticker like BTC, ETH, SOL)
- Returns: sentiment score (-1 to 1), confidence, sources, analysis
- Example: "What's the current sentiment for Ethereum?"

**x402_market_overview** ($0.05):
- No parameters
- Returns: market-wide sentiment, top movers, trend analysis
- Example: "Give me a broad overview of the crypto market right now"

**x402_intelligence** ($0.10):
- Params: coin (string, required — ticker like BTC, ETH, SOL)
- Returns: comprehensive multi-source analysis (CoinGecko, DeFiLlama, CryptoPanic, Fear & Greed, GitHub)
- Example: "Give me a deep intelligence report on Bitcoin"
- Note: "This is the premium tier — use x402_sentiment ($0.01) for quick sentiment only."

3. **Pricing Summary Table** at the end:
| Tool | Price | Free Test Mode |
|------|-------|---------------|
(All 6 tools with prices)

Add SYNC comment: `<!-- SYNC: prices must match src/index.ts and site/src/components/pricing/PricingTable.astro -->`

Voice: Sharp for reference. Precise, instructional. Parameter tables are JetBrains Mono domain.
  </action>
  <verify>
    <automated>cd /Users/jameswisdom/projects/x402-mcp-server/site && npm run build 2>&1 | tail -3 && test -f dist/api-reference/index.html && grep -c "x402_" src/content/docs/api-reference.mdx | xargs test 6 -le && echo "PASS: api-reference built with all tools"</automated>
  </verify>
  <done>
    - API reference documents all 6 MCP tools
    - Each tool has: description, cost, parameter table, return description, conversational example
    - Prices match src/index.ts exactly
    - SYNC comment present referencing src/index.ts
    - Builds without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Write wallet setup guide with step-by-step instructions</name>
  <files>
    site/src/content/docs/wallet-setup.mdx
    site/src/assets/wallet/placeholder-metamask-install.png
    site/src/assets/wallet/placeholder-add-base.png
    site/src/assets/wallet/placeholder-get-usdc.png
    site/src/assets/wallet/placeholder-export-key.png
  </files>
  <action>
Replace the placeholder wallet-setup.mdx with the complete wallet setup guide. Target audience: crypto newcomers who have never used MetaMask.

**Frontmatter:**
```yaml
---
title: Wallet Setup
description: Set up a crypto wallet to use paid x402 APIs — MetaMask, Base network, USDC funding, and private key export.
lastUpdated: 2026-03-11
---
```

**Structure** (using Steps component):

1. **Introduction**: "To use paid x402 APIs, you need a small amount of USDC (a US dollar stablecoin) on the Base network. This guide walks you through the complete setup — from installing a wallet to making your first paid API call."

2. **Step 1: Install MetaMask**
   - Download from metamask.io for your browser
   - Create a new wallet and save your recovery phrase securely
   - Use Aside type="danger": "Never share your recovery phrase or private key. Anyone with access can take your funds."
   - Placeholder image reference: `![MetaMask installation](../../assets/wallet/placeholder-metamask-install.png)`

3. **Step 2: Add Base Network**
   - Method 1 (easiest): Visit chainlist.org, search "Base", click "Add to MetaMask"
   - Method 2 (manual): Network Name: Base, RPC URL: https://mainnet.base.org, Chain ID: 8453, Currency: ETH, Explorer: https://basescan.org
   - Placeholder image reference
   - Note: "Base is a Layer 2 on Ethereum — transactions are fast and cheap."

4. **Step 3: Get USDC on Base**
   - Option A (if you have crypto): Bridge USDC from Ethereum to Base using bridge.base.org
   - Option B (if starting fresh): Buy ETH on Coinbase, send to MetaMask, use a DEX to swap to USDC, or buy USDC directly on Coinbase and withdraw to Base
   - Option C (minimal): "Even $1 of USDC covers 100 screenshot API calls. Start small."
   - Explain: "USDC is a stablecoin pegged 1:1 to USD. $1 USDC = $1 USD. No price volatility."
   - Placeholder image reference

5. **Step 4: Export Your Private Key**
   - In MetaMask: Click account icon → Account Details → Show Private Key → Enter password → Copy
   - Format: starts with `0x`, 66 characters total
   - Use Aside type="danger": "Your private key controls your wallet funds. Never paste it in chat, email, or any website. Only use it as an environment variable in your local MCP config."
   - Add the key to your MCP client config (link back to Getting Started paid mode section)
   - Placeholder image reference

6. **Security Tips**:
   - Use a dedicated wallet for x402 API payments — don't use your main crypto wallet
   - Fund with small amounts ($1-$5 covers thousands of API calls)
   - Never commit private keys to git — use environment variables
   - Rotate keys if you suspect exposure

**Placeholder images:**
Create 4 simple placeholder PNG files in `site/src/assets/wallet/`. These can be minimal 800x400 light gray images with centered text describing what the screenshot would show:
- `placeholder-metamask-install.png` — "MetaMask browser extension install screen"
- `placeholder-add-base.png` — "Adding Base network to MetaMask"
- `placeholder-get-usdc.png` — "USDC balance on Base network in MetaMask"
- `placeholder-export-key.png` — "Exporting private key from MetaMask"

If image generation tools are unavailable, create the asset directory and add a README noting that placeholder images are needed. The guide should still build and render without images (MDX gracefully handles missing referenced images with alt text).

Voice: Approachable, patient — this is for people who have never touched crypto. Walk them through like a friend.
  </action>
  <verify>
    <automated>cd /Users/jameswisdom/projects/x402-mcp-server/site && npm run build 2>&1 | tail -3 && test -f dist/wallet-setup/index.html && grep -q "MetaMask" src/content/docs/wallet-setup.mdx && grep -q "Base" src/content/docs/wallet-setup.mdx && grep -q "USDC" src/content/docs/wallet-setup.mdx && echo "PASS: wallet-setup built with full guide"</automated>
  </verify>
  <done>
    - Wallet setup guide covers: MetaMask install, Base network, USDC funding, private key export
    - Step-by-step using Starlight Steps component
    - Security warnings using Aside type="danger"
    - Placeholder image directory created at site/src/assets/wallet/
    - Links back to Getting Started guide for MCP config
    - lastUpdated frontmatter set for maintenance tracking
    - Builds without errors
  </done>
</task>

</tasks>

<verification>
1. `cd site && npm run build` exits 0
2. `dist/getting-started/index.html` exists and contains all 4 MCP client names
3. `dist/api-reference/index.html` exists and references all 6 tool IDs
4. `dist/wallet-setup/index.html` exists and mentions MetaMask, Base, USDC
5. `grep -r "npx x402-mcp-server" site/src/content/ | grep -v "\-y"` returns NO results (every npx has -y)
6. SYNC comments present in api-reference.mdx referencing src/index.ts
7. Cross-links work: getting-started links to wallet-setup and api-reference
</verification>

<success_criteria>
1. Getting Started guide enables a developer to install and use x402 MCP server in under 5 minutes (DOCS-01)
2. API reference documents all 6 tools with parameters, returns, pricing, and conversational examples (DOCS-02)
3. All 4 MCP client configs (Claude Desktop, Claude Code, Cursor, Windsurf) are present and copy-pasteable (DOCS-03)
4. Wallet setup guide walks through MetaMask → Base → USDC → private key export (DOCS-04)
5. Every npx invocation uses -y flag (verified by grep)
6. Common Issues section covers npx -y, @x402/fetch vs x402-fetch, insufficient USDC, and Node version
</success_criteria>

<output>
After completion, create `.planning/phases/03-brand-site-build/03-03-SUMMARY.md`
</output>
