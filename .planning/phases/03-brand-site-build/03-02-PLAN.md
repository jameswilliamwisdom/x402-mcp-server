---
phase: 03-brand-site-build
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - site/src/pages/index.astro
  - site/src/pages/pricing.astro
  - site/src/components/landing/Hero.astro
  - site/src/components/landing/HowItWorks.astro
  - site/src/components/landing/PricingSummary.astro
  - site/src/components/landing/Footer.astro
  - site/src/components/pricing/PricingTable.astro
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04

must_haves:
  truths:
    - "A developer landing on the homepage reads the one-liner pitch within 2 seconds"
    - "Homepage CTA 'Get Started' links to /getting-started/"
    - "Pricing table shows all 6 MCP tools with correct per-call USDC costs"
    - "How it works one-liner explains the x402 payment flow"
    - "OG meta tags on homepage use absolute URLs for title, description, and image"
    - "Pricing page at /pricing/ shows detailed tool breakdown"
  artifacts:
    - path: "site/src/pages/index.astro"
      provides: "Custom landing page with Hero, HowItWorks, PricingSummary, Footer"
      contains: "data-theme"
    - path: "site/src/pages/pricing.astro"
      provides: "Detailed pricing page with full tool table"
      contains: "PricingTable"
    - path: "site/src/components/landing/Hero.astro"
      provides: "Hero section with pitch, value props, CTA"
      contains: "Get Started"
    - path: "site/src/components/pricing/PricingTable.astro"
      provides: "Data-driven pricing table for all 6 tools"
      contains: "USDC"
  key_links:
    - from: "site/src/pages/index.astro"
      to: "site/src/components/landing/Hero.astro"
      via: "Astro component import"
      pattern: "import Hero"
    - from: "site/src/components/landing/Hero.astro"
      to: "/getting-started/"
      via: "CTA anchor href"
      pattern: "href.*getting-started"
    - from: "site/src/components/landing/PricingSummary.astro"
      to: "/pricing/"
      via: "Link to full pricing page"
      pattern: "href.*pricing"
---

<objective>
Build the custom landing page and pricing page — the two marketing pages that live outside Starlight. The homepage is the primary entry point for developers discovering x402. The pricing page provides the detailed tool-by-tool breakdown.

Purpose: These are the first pages a developer sees. They must communicate what x402 is, how it works, and what it costs within seconds.
Output: Fully styled landing page at / and pricing page at /pricing/ with all 6 tools' pricing data.
</objective>

<execution_context>
@/Users/jameswisdom/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jameswisdom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-brand-site-build/03-RESEARCH.md
@.planning/phases/03-brand-site-build/03-01-SUMMARY.md
@assets/brand-guidelines.md
@src/index.ts
</context>

<interfaces>
<!-- Pricing data extracted from src/index.ts — executor must match these exactly -->
<!-- SYNC: prices must match src/index.ts declarations -->

Tool pricing from src/index.ts:
| Tool Name | Tool ID | Price | Description |
|-----------|---------|-------|-------------|
| Network Info | x402_network_info | FREE | List all APIs with pricing, status, capabilities |
| Screenshot | x402_screenshot | $0.01 | Capture website screenshots as base64 images |
| PDF Extract | x402_pdf_extract | $0.01 | Extract text content from PDF documents |
| Sentiment | x402_sentiment | $0.01 | Real-time crypto sentiment for a specific coin |
| Market Overview | x402_market_overview | $0.05 | Broad crypto market sentiment overview |
| Intelligence | x402_intelligence | $0.10 | Comprehensive multi-source intelligence analysis |

Brand tokens available from global.css (Plan 03-01):
--x402-green: #4ADE80, --x402-green-muted: #22C55E, --x402-green-dim: #166534
--x402-green-glow: rgba(74, 222, 128, 0.15)
--x402-bg: #000000, --x402-surface: #111111, --x402-elevated: #1A1A1A
--x402-border: #2A2A2A, --x402-text: #FFFFFF, --x402-text-muted: #A1A1AA, --x402-text-dim: #52525B

Typography: Space Grotesk for headlines/marketing, JetBrains Mono for code/prices/endpoints.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build landing page components and index.astro</name>
  <files>
    site/src/pages/index.astro
    site/src/components/landing/Hero.astro
    site/src/components/landing/HowItWorks.astro
    site/src/components/landing/PricingSummary.astro
    site/src/components/landing/Footer.astro
  </files>
  <action>
Replace the placeholder index.astro from Plan 03-01 with the full custom landing page. Create all four landing page components.

**1. Hero.astro** (SITE-01):
- One-liner pitch: "Pay-per-use APIs for AI agents. One npm install, automatic USDC micropayments on Base."
- Value proposition bullets (3):
  - Free test mode — no wallet needed to start
  - Single env var — add X402_PRIVATE_KEY and go
  - Micropayments — $0.01 to $0.10 per request, no subscriptions
- Primary CTA button: "Get Started" linking to `/getting-started/` — styled with Protocol Green (#4ADE80) background
- Secondary CTA: "View Pricing" linking to `/pricing/`
- Logo mark displayed above the headline (from /logo-mark.png)
- Style: Space Grotesk for all text. Hero layout centered. Use --x402-green-glow for a subtle radial glow behind the logo.

**2. HowItWorks.astro** (SITE-03):
- Per user decision: one-liner ONLY on homepage, not a detailed section.
- Content: "HTTP 402 → USDC payment → API response. That's the whole protocol."
- Style as a highlighted statement using JetBrains Mono (it's describing the protocol flow — "speaks to machines").
- Visually separated from hero with --x402-border.

**3. PricingSummary.astro** (SITE-02 partial):
- Brief pricing overview: "6 tools. $0.01 to $0.10 per call. No subscriptions."
- Mini table or grid showing the 3 APIs with price ranges:
  - Screenshot API — $0.01
  - PDF Extraction — $0.01
  - Crypto Intelligence — $0.01 to $0.10
- Link: "See full pricing →" linking to `/pricing/`
- Prices rendered in JetBrains Mono per brand pairing rule.

**4. Footer.astro**:
- Minimal: "x402 Network" with link to GitHub repo (https://github.com/jameswilliamwisdom/x402-mcp-server)
- "Built for the AI agent economy."
- Style: --x402-text-dim color, small font.

**5. index.astro** (SITE-04):
- Full custom page — NOT Starlight template.
- Set `data-theme="dark"` on `<html>` element (dark mode enforcement for non-Starlight pages).
- Import `../styles/global.css` directly.
- Complete `<head>` with:
  - `<title>x402 — The API Marketplace for AI Agents</title>`
  - `<meta name="description" content="Pay-per-use APIs for AI agents. One npm install, automatic USDC micropayments on Base.">`
  - OG meta tags: og:title, og:description, og:image (absolute URL using `import.meta.env.SITE` or fallback), og:image:width (1200), og:image:height (630), og:type (website)
  - twitter:card (summary_large_image), twitter:image
  - Google Fonts link tags (Space Grotesk + JetBrains Mono)
  - Favicon: `/logo-mark.png`
- Body: Hero → HowItWorks → PricingSummary → Footer

Use Astro scoped `<style>` blocks for component-specific layout. Reference --x402-* tokens from global.css for colors. Use Space Grotesk as the default font family. Apply JetBrains Mono only on prices, endpoints, and protocol descriptions.

All landing page styles should be responsive: centered max-width container (~900-1000px), adequate padding on mobile.
  </action>
  <verify>
    <automated>cd /Users/jameswisdom/projects/x402-mcp-server/site && npm run build 2>&1 | tail -3 && grep -q "Get Started" dist/index.html && grep -q "og:image" dist/index.html && grep -q "getting-started" dist/index.html && echo "PASS: landing page built with hero + OG"</automated>
  </verify>
  <done>
    - Homepage at / renders Hero with pitch, value props, and "Get Started" CTA
    - "Get Started" links to /getting-started/
    - HowItWorks shows protocol one-liner in JetBrains Mono
    - PricingSummary shows 3 API categories with prices + link to /pricing/
    - Footer shows GitHub link and tagline
    - OG meta tags present with absolute URLs (og:title, og:description, og:image)
    - data-theme="dark" set on html element
    - All text uses correct brand fonts (Space Grotesk for marketing, JetBrains Mono for code/prices)
  </done>
</task>

<task type="auto">
  <name>Task 2: Build pricing page with full tool table</name>
  <files>
    site/src/pages/pricing.astro
    site/src/components/pricing/PricingTable.astro
  </files>
  <action>
Create the detailed pricing page at /pricing/ with a data-driven pricing table component.

**1. PricingTable.astro** (SITE-02):
Define a typed `Tool[]` array with all 6 MCP tools:

```typescript
interface Tool {
  name: string;
  toolId: string;
  price: string;
  description: string;
  freeMode: boolean;
}

// SYNC: prices must match src/index.ts — grep both files if pricing changes
const tools: Tool[] = [
  { name: "Network Info", toolId: "x402_network_info", price: "Free", description: "List all APIs with pricing, status, and capabilities", freeMode: true },
  { name: "Screenshot", toolId: "x402_screenshot", price: "$0.01", description: "Capture website screenshots as base64 images", freeMode: true },
  { name: "PDF Extract", toolId: "x402_pdf_extract", price: "$0.01", description: "Extract text content from PDF documents", freeMode: true },
  { name: "Sentiment", toolId: "x402_sentiment", price: "$0.01", description: "Real-time crypto sentiment for a specific coin", freeMode: true },
  { name: "Market Overview", toolId: "x402_market_overview", price: "$0.05", description: "Broad crypto market sentiment overview", freeMode: true },
  { name: "Intelligence", toolId: "x402_intelligence", price: "$0.10", description: "Comprehensive multi-source intelligence analysis", freeMode: true },
];
```

Render as a styled table or card grid:
- Tool name in Space Grotesk (bold)
- Tool ID (`x402_screenshot`) in JetBrains Mono
- Price in JetBrains Mono with Protocol Green color
- Description in Space Grotesk (regular weight, --x402-text-muted)
- "Free" badge for tools that work without a wallet (all tools have free test mode)
- Each row/card has subtle --x402-surface background with --x402-border

Add a note below the table: "All tools have free test mode — no wallet required to try them. Paid mode unlocks full capabilities."

**2. pricing.astro**:
- Full custom page (same approach as index.astro — NOT Starlight template).
- Set `data-theme="dark"` on `<html>`.
- Import `../styles/global.css`.
- `<head>` with title "Pricing — x402 Network", description, OG tags.
- Google Fonts links.
- Page heading: "Pricing" in Space Grotesk 48px bold.
- Subheading: "Pay only for what you use. No subscriptions, no API keys." in --x402-text-muted.
- Payment info section: "All payments in USDC on Base (L2 Ethereum). Sub-second settlement."
- Render PricingTable component.
- CTA at bottom: "Ready to start?" with button linking to /getting-started/
- Navigation link back to homepage.
  </action>
  <verify>
    <automated>cd /Users/jameswisdom/projects/x402-mcp-server/site && npm run build 2>&1 | tail -3 && test -f dist/pricing/index.html && grep -q "x402_screenshot" dist/pricing/index.html && grep -q '0.01' dist/pricing/index.html && grep -q '0.10' dist/pricing/index.html && echo "PASS: pricing page built with all 6 tools"</automated>
  </verify>
  <done>
    - Pricing page at /pricing/ renders all 6 MCP tools
    - Each tool shows name, tool ID, price, description, and free mode indicator
    - Prices match src/index.ts exactly: Free, $0.01, $0.01, $0.01, $0.05, $0.10
    - SYNC comments present in PricingTable.astro referencing src/index.ts
    - Page has proper OG tags and dark mode
    - CTA links to /getting-started/
  </done>
</task>

</tasks>

<verification>
1. `cd site && npm run build` exits 0
2. `dist/index.html` contains "Get Started" CTA linking to /getting-started/
3. `dist/index.html` contains og:image meta tag with absolute URL
4. `dist/pricing/index.html` contains all 6 tool IDs (x402_network_info through x402_intelligence)
5. Pricing values ($0.01, $0.05, $0.10) appear in pricing page matching src/index.ts
6. Both pages set data-theme="dark" on html element
7. No references to Starlight chrome on landing or pricing pages (these are custom Astro pages)
</verification>

<success_criteria>
1. Developer landing on / reads one-liner pitch and reaches Getting Started in one click (SITE-01)
2. Pricing table at /pricing/ shows all 6 tools with exact per-call costs matching src/index.ts (SITE-02)
3. Homepage shows "HTTP 402 → USDC payment → API response" protocol explanation (SITE-03)
4. Both pages have complete OG meta tags with absolute URLs for og:image (SITE-04)
5. Both pages enforce dark mode via data-theme="dark"
6. Brand typography applied: Space Grotesk for marketing copy, JetBrains Mono for prices/code
</success_criteria>

<output>
After completion, create `.planning/phases/03-brand-site-build/03-02-SUMMARY.md`
</output>
