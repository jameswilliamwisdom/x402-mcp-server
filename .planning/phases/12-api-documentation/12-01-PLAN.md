---
phase: 12-api-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/astro.config.mjs
  - site/src/content/docs/api-reference.mdx
  - site/src/content/docs/apis/scraping.mdx
  - site/src/content/docs/apis/file-conversion.mdx
  - site/src/content/docs/apis/web-search.mdx
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "Visiting /apis/scraping/ returns a page with parameter table, curl examples, MCP tool call example, and error codes for the Web Scraping API"
    - "Visiting /apis/file-conversion/ returns a page with parameter table, curl examples, MCP tool call example, and error codes for the File Conversion API"
    - "Visiting /apis/web-search/ returns a page with parameter table, curl examples, MCP tool call example, and error codes for the Web Search API"
    - "Each page shows the free test endpoint above the paid endpoint (BRAND-04)"
    - "The pricing table in api-reference.mdx shows correct prices: conversion $0.02, web search $0.01, email $0.01"
    - "All three new pages appear in the sidebar under an 'APIs' group"
  artifacts:
    - path: "site/src/content/docs/apis/scraping.mdx"
      provides: "Web Scraping API reference page"
      contains: "x402_scrape_url"
    - path: "site/src/content/docs/apis/file-conversion.mdx"
      provides: "File Conversion API reference page"
      contains: "x402_convert_file"
    - path: "site/src/content/docs/apis/web-search.mdx"
      provides: "Web Search API reference page"
      contains: "x402_web_search"
  key_links:
    - from: "site/astro.config.mjs"
      to: "site/src/content/docs/apis/*.mdx"
      via: "sidebar slug entries"
      pattern: "slug: 'apis/"
    - from: "site/src/content/docs/api-reference.mdx"
      to: "apis/*.mdx"
      via: "corrected pricing table consistent with new pages"
      pattern: "\\$0\\.02.*convert|\\$0\\.01.*search|\\$0\\.01.*email"
---

<objective>
Create three API reference pages (Web Scraping, File Conversion, Web Search), register all five API pages in the Starlight sidebar, and fix incorrect pricing in the existing api-reference.mdx.

Purpose: Delivers the first three of five required API reference pages (DOCS-01, DOCS-02, DOCS-03) and sets up the sidebar structure that Plan 12-02 will also use.
Output: Three new MDX files under `site/src/content/docs/apis/`, updated sidebar config in `astro.config.mjs`, corrected pricing table in `api-reference.mdx`.
</objective>

<execution_context>
@/Users/jameswisdom/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jameswisdom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-api-documentation/12-RESEARCH.md

Key source files for extracting exact schemas, error codes, and pricing:
@site/astro.config.mjs
@site/src/content/docs/api-reference.mdx

<interfaces>
{/* Sidebar config pattern — current structure in site/astro.config.mjs */}
sidebar: [
  { label: 'Getting Started', items: [{ slug: 'getting-started' }, { slug: 'wallet-setup' }] },
  { label: 'Reference', items: [{ slug: 'api-reference' }] },
]

{/* Per-page MDX frontmatter pattern */}
---
title: [API Name] API
description: [One-liner with price]
---
import { Aside } from '@astrojs/starlight/components';

{/* JSX comments only — HTML comments break MDX parser */}
{/* SYNC: price must match src/index.ts @pay() decorator and PricingTable.astro */}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sidebar config and fix pricing table</name>
  <files>site/astro.config.mjs, site/src/content/docs/api-reference.mdx</files>
  <action>
1. **Edit `site/astro.config.mjs`** — Add an "APIs" sidebar group after the existing "Reference" group. Register all 5 API page slugs (including the 2 that Plan 12-02 will create). The exact sidebar block to append after the Reference group:

```javascript
{
  label: 'APIs',
  items: [
    { slug: 'apis/scraping' },
    { slug: 'apis/file-conversion' },
    { slug: 'apis/web-search' },
    { slug: 'apis/email' },
    { slug: 'apis/audio-transcription' },
  ],
},
```

The slugs MUST exactly mirror file paths relative to `src/content/docs/` without extension. Plural `apis/` — not `api/`.

2. **Edit `site/src/content/docs/api-reference.mdx`** — Fix the pricing summary table (around lines 218–223). Three prices are wrong:
   - `x402_convert_file`: change `$0.05 USDC` to `$0.02 USDC`
   - `x402_web_search`: change `$0.02 USDC` to `$0.01 USDC`
   - `x402_send_email`: change `$0.02 USDC` to `$0.01 USDC`

3. Also in `api-reference.mdx`, replace the placeholder line "Full reference for scrape, convert, search, email, and transcription APIs coming in the next update." with links to each new page:

```
See the individual API pages for full reference: [Web Scraping](/apis/scraping/), [File Conversion](/apis/file-conversion/), [Web Search](/apis/web-search/), [Email Sending](/apis/email/), [Audio Transcription](/apis/audio-transcription/).
```

Use absolute paths with trailing slashes for all internal links (P14 pitfall — nginx redirect issue).
  </action>
  <verify>
    <automated>cd /Users/jameswisdom/projects/x402-mcp-server/site && grep -c "apis/scraping" astro.config.mjs && grep -c "apis/file-conversion" astro.config.mjs && grep -c "apis/web-search" astro.config.mjs && grep -c "apis/email" astro.config.mjs && grep -c "apis/audio-transcription" astro.config.mjs && grep '\$0\.02 USDC' src/content/docs/api-reference.mdx | grep -c 'convert' && grep '\$0\.01 USDC' src/content/docs/api-reference.mdx | grep -c 'search' && grep '\$0\.01 USDC' src/content/docs/api-reference.mdx | grep -c 'email'</automated>
  </verify>
  <done>
    - `astro.config.mjs` has an "APIs" sidebar group with all 5 slugs registered
    - `api-reference.mdx` pricing table shows correct prices: conversion $0.02, web search $0.01, email $0.01
    - Placeholder text replaced with links to the 5 new API pages
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Web Scraping, File Conversion, and Web Search API reference pages</name>
  <files>site/src/content/docs/apis/scraping.mdx, site/src/content/docs/apis/file-conversion.mdx, site/src/content/docs/apis/web-search.mdx</files>
  <action>
Create the `site/src/content/docs/apis/` directory if it doesn't exist. Then create three MDX files following the exact template from RESEARCH. Every page MUST:

- Use `import { Aside } from '@astrojs/starlight/components';`
- Use JSX comments only (`{/* */}`) — NEVER HTML comments (`<!-- -->`) (P1 confirmed bug)
- Show free test endpoint ABOVE paid endpoint (BRAND-04 / P5)
- Include a `{/* SYNC: price must match ... */}` JSX comment near each hardcoded price (P2)
- Use absolute paths with trailing slashes for internal links (P14)

---

**File 1: `site/src/content/docs/apis/scraping.mdx`** (DOCS-01)

Frontmatter:
```yaml
title: Web Scraping API
description: Scrape any URL and return structured markdown, links, tables, and metadata. $0.02 USDC per call.
```

Sections (follow template exactly):

**Endpoints:**
- Free: `GET /scrape/test` at `https://x402-scraping-api-production.up.railway.app`
- Paid: `POST /scrape` — $0.02 USDC on Base

**Aside:** "No API key required" tip with $0.02 price.

**Parameters:**

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `url` | string | Yes | — | Page to scrape (http/https, max 2048 chars) |
| `wait_for` | string | No | — | CSS selector to wait for before extraction (for SPAs) |

**curl examples:** Free test and paid (from RESEARCH).

**MCP tool call:**
> "Scrape the blog post at https://example.com/article and return the markdown content."
Tool: `x402_scrape_url`, arguments: `{"url": "https://example.com/article"}`

**Returns:** Describe: `success` (boolean), `markdown` (string — extracted page content), `links` (array of URLs found), `tables` (array of extracted tables), `metadata` (object with title, description, etc.). IMPORTANT: Note that scrape can return HTTP 200 with `success: false` — this is NOT an error HTTP code (P7). List the `error` field values: `timeout`, `browser_error`, `non_html_content`, `blocked_by_protection`.

**Error codes:**

| Code | Meaning |
|------|---------|
| 400 | SSRF blocked — URL targets a private/reserved IP range |
| 402 | Payment required — x402 handshake (handled automatically by MCP server) |
| 422 | Invalid URL or parameter validation failed |
| 429 | Rate limit exceeded (free test: 100/hour per IP) |
| 503 | Browser unavailable — scraping backend overloaded |

Also add a note after the error table: "HTTP 200 with `success: false` indicates the page loaded but extraction failed. Check the `error` field for: `timeout`, `browser_error`, `non_html_content`, `blocked_by_protection`."

---

**File 2: `site/src/content/docs/apis/file-conversion.mdx`** (DOCS-02)

Frontmatter:
```yaml
title: File Conversion API
description: Convert images, CSV, and HTML to different formats. $0.02 USDC per call.
```

**Endpoints:**
- Free: `GET /convert/test` at `https://x402-conversion-api-production.up.railway.app`
- Paid: `POST /convert` — $0.02 USDC on Base

**Parameters:** The `type` field is a discriminator. Use "(image only)" annotations in the Description column for conditional fields (P12). Do NOT use Tabs component.

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `type` | string | Yes | — | Conversion type: `"image"`, `"csv"`, or `"html_pdf"` |
| `url` | string | Yes | — | Source file URL (http/https, max 2048 chars, max 10MB) |
| `format` | string | No (image only) | `"jpeg"` | Output format: `"jpeg"`, `"png"`, `"webp"`, `"gif"` |
| `width` | integer | No (image only) | — | Target width in pixels (1–8000) |
| `height` | integer | No (image only) | — | Target height in pixels (1–8000) |

**curl example:** Show paid example with `type: "image"`.

**MCP tool call:**
> "Convert this image to PNG at 800px wide: https://example.com/photo.jpg"
Tool: `x402_convert_file`, arguments with `type: "image"`, `url`, `format: "png"`, `width: 800`.

**Returns:** All types return `data` as base64-encoded. For `type: "csv"`, base64-decode `data` to get a JSON array of objects (P8). For `type: "html_pdf"`, decode to get PDF binary. For `type: "image"`, decode to get image bytes.

**Error codes:**

| Code | Meaning |
|------|---------|
| 400 | SSRF blocked — URL targets a private/reserved IP range |
| 402 | Payment required — x402 handshake (handled automatically by MCP server) |
| 422 | Invalid URL, unsupported type, or parameter validation failed |
| 429 | Rate limit exceeded (free test: 100/hour per IP) |

Note after table: "HTTP 200 with `success: false` indicates download or conversion failure. Check the `error` field for: `download_error`, `http_error`, `conversion_error`, `output_too_large`."

---

**File 3: `site/src/content/docs/apis/web-search.mdx`** (DOCS-03)

Frontmatter:
```yaml
title: Web Search API
description: Search the web and return structured results with titles, URLs, and snippets. $0.01 USDC per call.
```

**Endpoints:**
- Free: `GET /search/test` at `https://x402-search-api-production.up.railway.app`
- Paid: `POST /search` — $0.01 USDC on Base

**Parameters:**

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `query` | string | Yes | — | Search query (1–400 characters) |
| `max_results` | integer | No | `5` | Number of results to return (1–10) |
| `include_answer` | boolean | No | `false` | Include a synthesized answer above search results |
| `include_domains` | string[] | No | — | Restrict results to these domains (max 20) |
| `exclude_domains` | string[] | No | — | Exclude results from these domains (max 20) |

Show actual defaults for optional params with defaults (P16): `max_results` → `5`, `include_answer` → `false`.

**curl example:** Free test + paid with `query` and `max_results`.

**MCP tool call:**
> "Search for recent news about Ethereum L2 scaling solutions, limit to 3 results."
Tool: `x402_web_search`, arguments: `{"query": "Ethereum L2 scaling solutions 2025", "max_results": 3}`

**Returns:** Array of results with `title`, `url`, `content` (snippet). If `include_answer: true`, also returns `answer` field.

**Rate limit note:** Add an `<Aside type="caution">` noting: "Limited to 50 queries per day per wallet, resetting at midnight UTC."

**Error codes:**

| Code | Meaning |
|------|---------|
| 402 | Payment required — x402 handshake (handled automatically by MCP server) |
| 422 | Invalid query or parameter validation failed |
| 429 | Daily wallet limit (50/day) or free test rate limit (100/hour per IP) exceeded |
| 500 | Search provider error |
| 503 | Search provider credits exhausted |

  </action>
  <verify>
    <automated>cd /Users/jameswisdom/projects/x402-mcp-server/site && test -f src/content/docs/apis/scraping.mdx && test -f src/content/docs/apis/file-conversion.mdx && test -f src/content/docs/apis/web-search.mdx && SITE_URL=https://usebismuth.com npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Three MDX files exist at `site/src/content/docs/apis/{scraping,file-conversion,web-search}.mdx`
    - Each file has: frontmatter, Aside import, Endpoints section (free above paid), parameter table, curl examples, MCP tool call example, Returns section, Error codes table
    - Astro build succeeds with no errors (confirms no MDX parser issues from HTML comments or bad syntax)
    - Free test endpoint appears above paid endpoint on every page (BRAND-04)
  </done>
</task>

</tasks>

<verification>
1. Run `cd /Users/jameswisdom/projects/x402-mcp-server/site && SITE_URL=https://usebismuth.com npx astro build` — must succeed with zero errors
2. Verify 5 sidebar slugs in `astro.config.mjs` — `grep -c "apis/" site/astro.config.mjs` should return 5
3. Verify pricing corrections in `api-reference.mdx` — conversion=$0.02, web search=$0.01, email=$0.01
4. Verify free-first ordering: `grep -n "Free test endpoint\|Paid endpoint" site/src/content/docs/apis/*.mdx` — free line number must be lower than paid line number on every page
5. Verify no HTML comments: `grep -rn '<!--' site/src/content/docs/apis/*.mdx` — must return empty
</verification>

<success_criteria>
- Astro build passes cleanly
- Three new API reference pages render correctly
- Sidebar shows all 5 API pages under "APIs" group (email and audio-transcription pages won't exist yet but their sidebar entries are registered)
- Pricing table in api-reference.mdx uses correct backend-verified prices
- No HTML comment syntax in any MDX file
</success_criteria>

<output>
After completion, create `.planning/phases/12-api-documentation/12-01-SUMMARY.md`
</output>
